const { getState, addBlock, getGenesisBlock, generateNextBlock, getLatestBlock } = require('./blockchain');

module.exports = {
  getState,
  addBlock,
  getGenesisBlock,
  generateNextBlock,
  getLatestBlock
}
